extends layout

block content
    form.form-horizontal(method="post")
        fieldset
            legend 用户注册
            .control-group
                label.control-label(for="username") 用户名
                .controls
                    input.input-xlarge#username(type="text", name="username")
                    p.help-block 你的账户名称，用于登录和显示,长度小于20个ASCII字符
            .control-group
                label.control-label(for="password") 密码
                .controls
                    input.input-xlarge#password(type="text", name="password")
                    p.help-block 请输入密码，长度小于20个ASCII字符
            .control-group
                label.control-label(for="password-repeat") 重复输入密码
                .controls
                    input.input-xlarge#password-repeat(type="text", name="password-repeat")
            .form-actions
                button.btn.btn-primary(type="submit") 注册
